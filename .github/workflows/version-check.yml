# Generated by scitrera-repo-tools `generate-ci-gha`. Do not edit by hand;
# re-run the generator after editing versions.yaml. Run
# `generate-ci-gha` (no flags) to check for drift, `generate-ci-gha --force` to apply.

name: Version Check

on:
  pull_request:
    paths:
      - '.github/workflows/**'
      - 'memorylayer-admin/package.json'
      - 'memorylayer-data-connectors/pyproject.toml'
      - 'memorylayer-embed-server-enterprise/pyproject.toml'
      - 'memorylayer-enterprise/pyproject.toml'
      - 'versions.yaml'

permissions:
  contents: read

jobs:
  check:
    name: sync-versions --check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v10.0.1

      - name: Verify versions are in sync
        run: uvx --from 'scitrera-repo-tools==0.1.29' sync-versions --check --verbose

      - name: Verify generated workflows
        run: uvx --from 'scitrera-repo-tools==0.1.29' generate-ci-gha --check
